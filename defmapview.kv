#:kivy 2.0.0
#:import MapView kivy.garden.mapview.MapView

<DefMapView>:
    lat: 10.0
    lon: 10.0
    zoom: 17
    map_source: "osm"
    pause_on_action: False
    double_tap_zoom: True
    on_zoom:
        self.get_controls_on_zoom()
    on_lat:
        self.on_start()