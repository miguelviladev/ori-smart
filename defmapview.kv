#:kivy 2.0.0
#:import MapView kivy.garden.mapview.MapView

<DefMapView>:
    lat: 40.638586
    lon: -8.654022
    zoom: 18
    pause_on_action: False
    double_tap_zoom: True
    on_zoom:
        self.zoom = 15 if self.zoom < 15 else self.zoom
    on_lat:
        self.start_getting_controls_in_fov()
    on_lon:
        self.start_getting_controls_in_fov()